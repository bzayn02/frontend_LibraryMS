import React from 'react';
import UserLayout from '../../components/layout/UserLayout';

const BorrowHistory = () => {
  return <UserLayout title="Borrow History">Borrow History</UserLayout>;
};

export default BorrowHistory;
